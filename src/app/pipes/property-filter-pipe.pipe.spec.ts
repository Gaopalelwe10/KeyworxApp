import { PropertyFilterPipePipe } from './property-filter-pipe.pipe';

describe('PropertyFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});

import { LocationFiterPipePipe } from './location-fiter-pipe.pipe';

describe('LocationFiterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocationFiterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
